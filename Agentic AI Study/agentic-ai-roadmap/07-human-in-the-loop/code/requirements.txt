# Phase 7 — Human in the Loop (HITL)
# Install with:  pip install -r requirements.txt
#
# Java analogy: this is your pom.xml / build.gradle dependency block.
# Pinning (==) keeps builds reproducible like a lockfile.
#
# NOTE: both scripts ship with USE_MOCK = True, so you can run them OFFLINE
# with NO API key. langchain-anthropic is only exercised when USE_MOCK = False.
# uvicorn is only needed to serve 02 for real; the offline smoke test uses
# FastAPI's in-process TestClient (no server, no network).

langgraph==0.2.60            # StateGraph + MemorySaver + interrupt_before (the HITL engine)
langchain-core==0.3.28       # message types (HumanMessage, AIMessage)
langchain-anthropic==0.3.1   # ChatAnthropic wrapper — only used when USE_MOCK = False
fastapi==0.115.6             # the draft/review HTTP API (your Spring @RestController)
uvicorn==0.34.0              # ASGI server to run FastAPI for real (not needed for the smoke test)
python-dotenv==1.0.1         # loads .env into env vars — like application.properties
