# Phase 9 — Debugging & Observability
# Install:  pip install -r requirements.txt
#
# NOTE: every demo in this folder runs OFFLINE with no API key. These packages are
# only needed to point the demos at the REAL LangSmith / LangGraph / Anthropic
# backends (see the TODO(real) markers in each .py file). The structured-logging
# demo (03) uses only the Python stdlib and needs none of these.

langsmith>=0.1.0        # 9.1 tracing backend — the "Jaeger" of agents (@traceable lives here)
langgraph>=0.2.0        # 9.2 real graph with get_state / get_state_history + a checkpointer
langchain-core>=0.3.0   # shared primitives used by langgraph / langsmith
anthropic>=0.40.0       # 9.4 real responses carrying response.usage.input_tokens/output_tokens
