# Phase 0 — Python Foundations
# Core dependencies needed to run every example in this phase.
# Install with:  pip install -r requirements.txt
#
# Java analogy: this file is your pom.xml / build.gradle dependency block.
# Pinning (==) keeps builds reproducible the same way a lockfile does.

fastapi==0.115.6        # Web framework — the @RestController + Spring MVC equivalent
uvicorn==0.34.0         # ASGI server — like an embedded Tomcat/Netty for FastAPI
httpx==0.28.1           # Async HTTP client — the WebClient / RestTemplate of Python
pydantic==2.10.4        # Validated DTOs / models — Bean Validation + Jackson combined
python-dotenv==1.0.1    # Loads .env into env vars — like Spring's application.properties
