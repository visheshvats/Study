# Phase 4 — Workflows
# Install with:  pip install -r requirements.txt
#
# Java analogy: this is your pom.xml / build.gradle dependency block.
# Pinning (==) keeps builds reproducible like a lockfile.
#
# NOTE: every example ships with USE_MOCK = True, so you can run all three
# scripts with ONLY langgraph + langchain-core installed and NO API key.
# 01_prompt_chaining.py uses LCEL (langchain-core); 02_router_pattern.py and
# 03_parallelization.py use LangGraph. langchain-anthropic is only needed when
# you flip USE_MOCK = False.

langgraph==0.2.60            # StateGraph + conditional edges (router, fan-out/fan-in)
langchain-core==0.3.28       # LCEL (prompt | llm | parser), message types, StrOutputParser
langchain-anthropic==0.3.1   # ChatAnthropic wrapper — only used when USE_MOCK = False
python-dotenv==1.0.1         # loads .env into env vars — like Spring's application.properties
