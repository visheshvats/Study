"""
FastAPI surface for the blog generator. See Phase 0 section 0.5.
Run:  uvicorn app:app --reload
"""
from __future__ import annotations

# TODO: from fastapi import FastAPI
# TODO: from pydantic import BaseModel
# TODO: import workers, orchestrator

# app = FastAPI(title="Blog Generator")


# class GenerateRequest(BaseModel):
#     topic: str
#     word_count: int = 300


# @app.post("/generate")
# async def generate(req: GenerateRequest):
#     # TODO: build Researcher/Writer/Editor, Orchestrator([...]).run(goal), return {article}
#     raise NotImplementedError


if __name__ == "__main__":
    print("Fill in /generate, then run: uvicorn app:app --reload")
